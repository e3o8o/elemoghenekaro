{
  "name": "preterag-site",
  "version": "1.0.0",
  "description": "Preterag website",
  "scripts": {
    "start": "npx http-server . -p 4001 -c-1",
    "generate": "node generate-posts.js",
    "build:writing": "node writing/build.js",
    "build:all": "npm run generate && npm run build:writing"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^15.0.7"
  }
}
